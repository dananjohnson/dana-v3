<nav>
  <ul>
    {% for link in nav.header %}
      <li>
        <a href="{{ link.path | url }}{{ "/1" if link.paginated else "" }}"{% if link.path == page.url %} aria-current="page"{% endif %}>
          {{ link.title }}
        </a>
      </li>
    {% endfor %}
  </ul>
</nav>
