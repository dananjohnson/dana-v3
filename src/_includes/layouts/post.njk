{% extends "./base.njk" %}

{% block content %}
<article>
  <header>
    <h1>{{ title }}</h1>
    <time datetime="{{ page.date | w3Date }}">{{ page.date | displayDate }}</time>
  </header>
  <div>{{ content | safe }}</div>
</article>
{% endblock %}
